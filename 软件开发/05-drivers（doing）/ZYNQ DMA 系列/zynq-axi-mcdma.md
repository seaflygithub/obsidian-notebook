


**为什么需要“多通道”？**（核心）

支持“多数据流”（最本质目的），例如：
```txt
多通道 ≠ 多个 DMA IP
多通道 = 一个 DMA + 多个独立上下文

ADC0 → DMA → DDR
ADC1 → DMA → DDR
Video → DMA → DDR
一个流卡住 ≠ 影响其他流
```

如果只有单通道，**所有流必须排队**。

多通道提高“有效带宽利用率”，注意不是“带宽变大”，而是**更容易把带宽用满**。










# Bottom






