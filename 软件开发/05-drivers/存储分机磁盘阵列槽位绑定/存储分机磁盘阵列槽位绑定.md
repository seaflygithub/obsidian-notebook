

1、问题概述: 存储分机有很多磁盘，会有磁盘的插拔操作，或者换盘等物理操作，以及常规的开机关机操作。比如A槽位的有磁盘，此时A槽位的磁盘在系统中的盘符为sda，但是在插拔盘等物理操作后，或者下次开关机之后，该槽位对应的盘符会随机改变，比如sda变成了sdc。经过初步分析，这是由于Linux系统启动时，这些槽位的磁盘，谁先被probe，谁就优先获取盘符，且盘符是根据字母顺序递增分配。这会导致一个比较严重的问题，在依赖于盘符的存储应用服务中，这会导致数据落盘错乱，本来这次往sda写入数据，下次继续往sda写入数据时，这时的sda已经不是之前对应的磁盘了。

2、因此这里需要解决，至少要把物理槽位和盘符绑定，以便后续的存储维护。

3、参考文章：[[50263083]]

4、目前已经实现的方案，就是在expander的基础上，获取每个物理槽位的ID信息，然后把ID和盘符进行固定关联，这样就能达到盘符和槽位绑定。

5、附件：网盘：arrowpoint_expander_master_v202404121900.zip

如下图所示，需要绑定物理磁盘槽位与软件上的逻辑分区，比如下图中最左下角对应的软件逻辑分区为 /dev/sdma，那么即使它掉盘了，即使该物理槽位换了个新硬盘了，其对应的逻辑分区应该仍然为 /dev/sdma 分区，而不能变动。

![[Pasted image 20251222173532.png]]

在前端页面展示的效果图：
![[Pasted image 20251222173544.png]]






