

# 根文件系统制作


## busybox最小文件系统

### 准备工作

- busybox源代码官方：https://busybox.net/downloads/
- Ubuntu安装必要的库：```apt install libncurses5-dev```

### 支持中文
我们想要busybox根文件系统能支持中文显示，比如ls能显示中文名的文件。需要修改源代码：vim libbb/printable_string.c，在文件中找到 ENABLE_UNICODE_SUPPORT 宏判断处，屏蔽掉或者注释掉关于0x7f的处理代码块即可。

然后编辑 vim libbb/unicode.c 文件，找到函数 unicode_conv_to_printable2 ，把其中关于大于 0x7f 的操作语句注释掉即可。至此，busybox源码就可以支持中文字符了，但还需要配置 busybox 来使能 unicode 码，这个稍 后会去配置。

### 编译配置
配置菜单项：
```bash
make defconfig
make menuconfig ARCH=arm CROSS_COMPILE=arm-linux-
```

```txt
Settings --->
    --- Build Options
        (arm-linux-gnueabihf-)Cross compiler prefix
    --- Installation Options
        (./_install) Destination path for 'make install' (NEW)
[*] vi-style line editing commands(NEW)

Busybox Configuration
    Linux Module Utilities --->
        [ ] Simplified modutils

Settings --->
    Support Unicod
         [*]Support Unicod
         [*]Check $LC_ALL, $LC_CTYPE and $LANG environment variables
```

### 创建目录树
安装编译好的文件：
```bash
make install
cd rootfs
mkdir dev etc lib mnt proc sys tmp var
```

添加网络配置文件：
```bash
vim etc/eth0-setting
    IP=192.168.5.20
    Mask=255.255.255.0
    Gateway=192.168.5.1
    DNS=192.168.5.1
    MAC=08:90:90:90:90:90

chmod 755 etc/eth0-setting

mkdir -p etc/init.d
vim etc/init.d/ifconfig-eth0
        #!/bin/bash
        echo -n try to bring eth0 interface up ... > /dev/ttymxc0

        if [ -f /etc/eth0-setting ] ; then
            source /etc/eth0-setting
            if grep -q "^/dev/root / nfs " /etc/mtab ; then
                echo -n NFS root ... > /dev/ttymxc0
            else
                ifconfig eth0 down
                ifconfig eth0 hw ether $MAC
                ifconfig eth0 $IP netmask $Mask up
                route add default gw $Gateway
            fi
            echo nameserver $DNS > /etc/resolv.conf
        else
            if grep -q "^/dev/root / nfs " /etc/mtab ; then
                echo -n NFS root ... > /dev/ttymxc0
            else
                /sbin/ifconfig eth0 192.168.5.20 netmask 255.255.255.0 up
            fi
        fi
        echo Done > /dev/ttymxc0

chmod 755 etc/init.d/ifconfig-eth0
```

添加开机自启动文件：
```bash
vim etc/init.d/rcS
        #!/bin/sh
        PATH=/sbin:/bin:/usr/sbin:/usr/bin:/usr/local/bin
        runlevel=S
        prevlevel=N
        umask 022

        export PATH runlevel prevlevel

        trap ":" INT QUIT TSTP
        /bin/hostname iTOP-iMX6UL

        [ -e /proc/1 ] || /bin/mount -n -t proc none /proc
        [ -e /sys/class ] || /bin/mount -n -t sysfs none /sys
        [ -e /dev/tty ] || /bin/mount -t ramfs none /dev

        echo /sbin/mdev > /proc/sys/kernel/hotplug
        /sbin/mdev -s

        mkdir -p /dev/pts
        mkdir -p /dev/shm
        /bin/mount -n -t devpts none /dev/pts -o mode=0622
        /bin/mount -n -t tmpfs tmpfs /dev/shm

        mkdir -p /var/empty
        mkdir -p /var/log
        mkdir -p /var/log/boa
        mkdir -p /var/lock
        mkdir -p /var/run
        mkdir -p /var/tmp

        #ln -sf /dev/ttyS2 /dev/tty2
        #ln -sf /dev/ttyS2 /dev/tty3
        #ln -sf /dev/ttyS2 /dev/tty4

        syslogd
        /etc/rc.d/init.d/netd start

        echo " " > /dev/tty1
        echo "Starting networking..." > /dev/tty1

        #/etc/rc.d/init.d/httpd start
        mkdir /mnt/disk
        /sbin/ifconfig lo 127.0.0.1
        /etc/init.d/ifconfig-eth0

chmod 755 etc/init.d/rcS
```

建立密码文件：
```bash
vim etc/passwd
        root::0:0:root:/:/bin/sh
        bin:*:1:1:bin:/bin:
        daemon:*:2:2:daemon:/sbin:
        nobody:*:99:99:Nobody:/:

chmod 755 etc/passwd
```

建立用户登录配置文件：
```bash
vim etc/profile
        ulimit -S -c 0 > /dev/null 2>&1
        USER="`id -un`"
        LOGNAME=$USER
        PS1='[$USER@$HOSTNAME]# '
        PATH=$PATH
        HOSTNAME=`/bin/hostname`
        export USER LOGNAME PS1 PATH

chmod 755 etc/profile
```

建立网络服务启动文件
```bash
mkdir -p etc/rc.d/init.d
vim etc/rc.d/init.d/netd
        #!/bin/sh
        base=inetd
        case "$1" in
            start)
                /usr/sbin/$base
                ;;
            stop)
                pid=`/bin/pidof $base`
                if [ -n "$pid" ]; then
                    kill -9 $pid
                fi
                ;;
        esac
        exit 0

chmod 755 etc/rc.d/init.d/netd
```

拷贝C语言运行时库：
```bash
cp -d /xxx/gcc-linaro-4.9.4-xxx/arm-linux-gnueabihf/libc/lib/*  lib/

cd var
mkdir lib lock log run tmp
```

### 打包文件
```bash
tar -cjf rootfs.tar.bz2 bin dev etc lib linuxrc mnt proc sbin sys tmp usr var
```

### 烧写文件系统
制作完成，根据不同开发板的烧写操作，把该根文件系统烧写至开发板。

### 功能测试
- 挂载U盘测试
- 编写helloworld程序运行测试（```xxx-gcc hello.c -o hello.elf -static```）
- 广域网连接测试（修改前面的网络配置文件即可）
- 中文显示测试（在xshell或者minicom上新建中文文件）

## buildroot文件系统

文件系统通常要包含很多第三方软件，为了避免繁杂的移植工作。buildroot 应运而生。之前通过 busybox 搭建的文件系统，需要我们自己去添加库文件，初始化脚本等，我们直接使用 buildroot 可以省去很多步骤，而且 buildroot 集成了很多第三方软件。Buildroot 是 Linux 平台上一个构建嵌入式 Linux 系统的框架。整个 Buildroot 是由 Makefile 脚本和 Kconfig 配置文件构成的。你可以和编译 Linux 内核一样，通过 buildroot 配置，menuconfig 修改，编译出一个完整 的可以直接烧写到机器上运行的 Linux 系统软件(包含 boot、kernel、rootfs 以及 rootfs 中的各种库和应用程 序)。

### 准备工作

下载地址官方：https://buildroot.org/

### 配置源码
```bash
mkdir -p buildroot
cd buildroot
tar -xjf buildroot-2019.02.9.tar.bz2
make menuconfig

# begin
Target options --->
    -> Target Architecture = ARM (little endian)
    -> Target Binary Format = ELF
    -> Target Architecture Variant = cortex-A7
    -> Floating point strategy = NEON/VFPv4
    -> ARM instruction set = ARM
Toolchain --->
    Toolchain type (External toolchain)
        Toolchain (Custom toolchain) --->
        Toolchain origin (Pre-installed toolchain) --->
        (/xxx/gcc-linaro-4.9.4-2017.01-x86_64_arm-linux-gnueabihf/) Toolchain path
        ($(ARCH)-linux-gnueabihf) Toolchain prefix
        External toolchain gcc version (4.9.x) --->
        External toolchain kernel headers series (4.1.x) --->
        External toolchain C library (glibc/eglibc) --->
        [*] Toolchain has SSP support? (NEW)
        [*] Toolchain has RPC support? (NEW)
        [*] Toolchain has C++ support?
        [*] Enable MMU support (NEW)
System configuration --->
    (iTOP_imx6ul/ull) System hostname
    (Welcome to topeet!) System banner
    Passwords encoding (sha-256) --->
    Init system (BusyBox) --->
    /dev management (Dynamic using devtmpfs + mdev) --->
    (system/device_table.txt) Path to the permission tables
    [ ] Use symlinks to /usr for /bin, /sbin and /lib
    [*] Enable root login with password
    (topeet) Root password
    /bin/sh (busybox' default shell) --->
    [*] Run a getty (login prompt) after boot --->
        (/dev/ttymxc0) TTY port
        Baudrate (115200) --->
        (vt100) TERM environment variable
    [*] remount root filesystem read-write during boot
    () Network interface to configure through DHCP
    (/bin:/sbin:/usr/bin:/usr/sbin) Set the system's default PATH
    [*] Purge unwanted locales
    (C en_US) Locales to keep
Filesystem images --->
    *** initramfs needs a Linux kernel to be built ***
    [*] tar the root filesystem
    Compression method (bzip2) --->
#end of configure

# compile (will download some sw source packet during this time)
make

# compile error fix
vim /xxx/arm-linux-xxx/arm-linux-gnueabihf/libc/usr/include/linux/version.h
    LINUX_VERSION_CODE  --> 262400
make

ls /xxx/buildroot/buildroot-2019.02.9/output/images/
    rootfs.tar.bz2
```

### 功能测试

开发板启动模式换成烧写模式，上电烧写。

### 添加支持第三方软件

通过 buildroot 配置使能这些软件和库。大部分都在配置界面的"Target packages"选项内。这些库和软件的源码也是在编译过程中从网络上下载的，所以要确保虚拟机的网络状态 是可以联网的。

比如下面使能音频库：

```bash
make menuconfig

# 使能 alsa-lib：
Target packages --->
    Libraries --->
        Audio/Sound --->
            [*] alsa-lib --->
                (/dev/snd) directory with ALSA device files (NEW)
                (all) built PCM plugins
                (all) built control plugins
                [*]后面选项全部星号使能

# 使能 alsa-utils：
Target packages --->
    Audio and video applications --->
        [*] alsa-utils --->
            [*] 后面选项全部星号使能

make
```

### buildroot下配置busybox

Buildroot 也是基于 busybox 制作的文件系统，所以我们 buildroot 里的 busybox 也可以进行配置。配置流程如下：

```bash
make busybox-menuconfig
# 配置方法可参考前面busybox章节

# 单独编译
make busybox
```

## yocto文件系统

虽然市面上有数百种可用的嵌入式 Linux 发行版，但每个版本都是高度定制的，这导 致了大量的重复工作，很多开发人员创建了大同小异的 Linux 版本，再基于其开发自己的增值特性和功能。Yocto 是一个开源社区。它通过提供模版、工具和方法帮助开发者创建基于 linux 内核的定制系统，支 持 ARM,PPC,MIPS,x86(32&64bit)硬件体系架构。Yocto Project 为解决嵌入式 Linux 开发乱象迈出了一大步。芯片、操作系统和设备厂商之间的协作和战 略规划有助于实现广泛而一致的行业支持。因此，操作系统厂商可以基于 Yocto Project 内核来开发商业操作 系统和其他产品，而且芯片厂商也可以提供硬件 BSP。

该项目有以下优势：健壮的构建系统。Yocto Project 支持从其他常用构建系统轻松迁移到 Yocto Project 的经过事实验证的环 境，而且新的软件版本更新也十分容易。灵活的定制能力。开发人员可以自由选择资源的使用，或者完善特定的组件(包括这些组件的特定版本)。Yocto Project 的设计允许用户使用自主开发的操作系统或商业操作系 统，在迁移时不会丢失优化或功能。综上所述，Yocto Project 赋予开发人员一个极高的起点，帮助他们避免重复工作以及不必要的维护。事 实上，Yocto Project 是全球最大的开源项目之一，以此为基础而开发的通用创建系统，可帮助开发人员避免 从零开始，转而把时间和资源集中于开发差异化 Linux 设备，从而实现投资回报最大化。

### 准备工作

ubuntu14.04 及以上版本64位，ubuntu 内存 8G 以上，越大越好，作者环境是 8G。保证硬盘剩余空间至少 80G。yocto 构建时会在线下载很多文件，我们需要要保证 ubuntu 网络环境。所以操作均在非 root 用户下执行。

ubuntu中安装必要环境：
```bash
sudo apt-get install -y git
git config --global user.name "Your Name"
git config --global user.email "Your Email"
git config --list

sudo apt-get install -y gawk wget git-core diffstat 
sudo apt-get install -y unzip texinfo 
sudo apt-get install -y gcc-multilib build-essential 
sudo apt-get install -y chrpath socat libsdl1.2-dev

sudo apt-get install -y libsdl1.2-dev xterm sed cvs subversion
sudo apt-get install -y coreutils texi2html docbook-utils 
sudo apt-get install -y python-pysqlite2 help2man 
sudo apt-get install -y make gcc g++ desktop-file-utils 
sudo apt-get install -y libgl1-mesa-dev libglu1-mesa-dev 
sudo apt-get install -y mercurial autoconf automake 
sudo apt-get install -y groff curl lzop asciidoc
```

### 获取repo

获取repo:\
方法一，官方提供的下载地址，需要翻墙: 
http://commondatastorage.googleapis.com/git-repo-downloads/repo

方法二，笔者在网上找到一个下载地址:
https://mirrors.tuna.tsinghua.edu.cn/git/git-repo

```bash
mkdir -p ~/imx6ull/yocto ; cd ~/imx6ull/yocto

# download repo
curl https://mirrors.tuna.tsinghua.edu.cn/git/git-repo -o repo
chmod a+x repo
export REPO_URL='https://mirrors.tuna.tsinghua.edu.cn/git/git-repo/'
export PATH=`pwd`:$PATH

# sync repo
mkdir ~/fsl-release-bsp ; cd ~/fsl-release-bsp
repo init -u git://git.freescale.com/imx/fsl-arm-yocto-bsp.git -b imx-4.1-krogoth
repo sync


# compile yocto project
DISTRO= MACHINE= source fsl- setup-release.sh -b
    fsl-imx-x11        <---
    fsl-imx-wayland
    fsl-imx-xwayland
    fsl-imx-fb

    imx6qpsabreauto
    imx6qpsabresd
    imx6ulevk
    imx6ull14x14evk
    imx6ull9x9evk
    imx6dlsabreauto
    imx6dlsabresd    <---
    imx6qsabreauto
    imx6qsabresd
    imx6slevk
    imx6solosabreauto
    imx6solosabresd
    imx6sxsabresd
    imx6sxsabreauto
    imx7dsabresd

DISTRO=fsl-imx-x11 MACHINE=imx6qsabresd source fsl-setup-release.sh -b build

vim build-dir/conf/local.conf
    BB_NUMBER_THREADS = '8'
    PARALLEL_MAKE = '-j 4'

# compile image files
bitbake fsl-image-qt5
    core-image-minimal
    core-image-base
    core-image-sato
    fsl-image-machine-test
    fsl-image-gui
    fsl-image-qt5

# 编译过程漫长，如果中途出错或者不小心关闭了终端
# 那么重新打开终端后，进入 fsl-release-bsp 后执行：
source setup-environment build

# 编译结束后会占用80G左右空间，虚拟机默认配置空间会不足

# 编译qt5的交叉编译工具链
bitbake meta-toolchain-qt5

# 编译完成后会在/opt/yocto/fsl-release-bsp/build/tmp/deploy/sdk目录下生成文件
# fsl-imx-fb-glibc-i686-meta-toolchain-qt5-cortexa9hf-neon-toolchain-4.1.15-2.0.0.sh
cd /opt/yocto/fsl-release-bsp/build/tmp/deploy/sdk
./fsl-imx-x11-glibc-x86_64-meta-toolchain-qt5-cortexa9hf-neon-toolchain-4.1.15-2.1.0.sh
. /opt/fsl-imx-x11/4.1.15-2.1.0/environment-setup-cortexa9hf-neon-poky-linux-gnueabi

# 执行如下命令安装GCC的交叉编译工具链
source setup-environment build
bitbake meta-toolchain
cd ~/fsl-release-bsp/build/tmp/deploy/sdk
./fsl-imx-x11-glibc-x86_64-meta-toolchain-cortexa9hf-neon-toolchain-4.1.15-2.1.0.sh

# 一路选择默认设置，最终将在/opt/fsl-imx-x11/4.1.15-2.1.0/目录下生成我们所需要的工具链。
. /opt/fsl-imx-x11/4.1.15-2.1.0/environment-setup-cortexa9hf-neon-poky-linux-gnueabi

# 至此，我们所需要的所有文件均已生成，
# 包含了uboot，kernel，rootfs以及交叉编译qt应用程序的交叉编译工具。
# qt5.6.2 当然最重要的是yocto编译生成的rootfs中包括了imx6所需要的各种库，
# 特别是有我们所需要的能完整的支持imx6的gpu性能的qt5.6

# 设置开机自动运行脚本
cd /opt/fsl-imx-x11/4.1.15-2.1.0
chmod a+x environment-setup-cortexa9hf-neon-poky-linux-gnueabi
vim ~/.bashrc
    source /opt/fsl-imx-x11/4.1.15-2.1.0/environment-setup-cortexa9hf-neon-poky-linux-gnueabi
```


## ubuntu文件系统

不带界面的 ubuntu-base 版本，Ubuntu Base 是用于为特定需求创建自定义映像的最小 rootfs。Ubuntu 移植比较简单，ubuntu 官方提供了制作好的根文件系统，我们只需要配置下相关内容即可放到 板子上运行。

### 准备工作

Ubuntu 官方针对不同的 CPU 架构提供相应的 ubuntu base 根文件系统，有 amd64(64 位 X86)、armhf、 i386(32 位 X86)、powerpc、ppc64el 等系统的：http://cdimage.ubuntu.com/ubuntu-base/releases/16.04.5/release/

```bash
su - root
mkdir -p ~/imx6ull/ubuntu-base ; cd ~/imx6ull/ubuntu-base
wget http:xxx/release/ubuntu-base-16.04.5-base-armhf.tar.gz

tar -xzf ubuntu-base-16.04.5-base-armhf.tar.gz

# 安装 qemu-user-static
apt-get install -y qemu-user-static
cp /usr/bin/qemu-arm-static ./usr/bin/

# 设置软件源
# 为了制作成功的根文件系统能够联网，
# 可以直接拷贝本机的 dns 配置文件到根文件系统的相应位置
cp /etc/resolv.conf ./etc/resolv.conf

# 添加 ubuntu arm 源，我选择的是中科大的源，在文末添加如下内容：
vim ./etc/apt/sources.list
deb http://mirrors.ustc.edu.cn/ubuntu-ports/ xenial main multiverse restricted universe 
deb http://mirrors.ustc.edu.cn/ubuntu-ports/ xenial-backports main multiverse restricted universe 
deb http://mirrors.ustc.edu.cn/ubuntu-ports/ xenial-proposed main multiverse restricted universe 
deb http://mirrors.ustc.edu.cn/ubuntu-ports/ xenial-security main multiverse restricted universe 
deb http://mirrors.ustc.edu.cn/ubuntu-ports/ xenial-updates main multiverse restricted universe 
deb-src http://mirrors.ustc.edu.cn/ubuntu-ports/ xenial main multiverse restricted universe 
...
# deb 前缀改成 deb-src 前缀表示获取软件源代码

# 挂载根文件系统并 Chroot
vim mount.sh
    #!/bin/bash
    echo "MOUNTING"
    mount -t proc /proc /home/topeet/ubuntu-base/proc
    mount -t sysfs /sys /home/topeet/ubuntu-base/sys
    mount -o bind /dev /home/topeet/ubuntu-base/dev
    mount -o bind /dev/pts /home/topeet/ubuntu-base/dev/pts
    chroot /home/topeet/ubuntu-base/
vi umount.sh
    #!/bin/bash
    echo "UNMOUNTING"
    sudo umount /home/topeet/ubuntu-base/proc
    sudo umount /home/topeet/ubuntu-base/sys
    sudo umount /home/topeet/ubuntu-base/dev/pts
    sudo umount /home/topeet/ubuntu-base/dev

chmod 777 mount.sh umount.sh
./mount.sh

# 此时我们可以看到，根目录切换成了当前制作 ubuntu 的目录
# 由于 ubuntu base 是一个最小根文件系统，很多命令和软件都没有，
# 因此我们需要先安装一下常用的 命令和软件：
apt update 
apt install sudo
apt install language-pack-en-base
apt install ssh
apt install net-tools
apt install ethtool
apt install ifupdown
apt install iputils-ping
apt install rsyslog
apt install htop
apt install vim

# 修改 root 密码
passwd root

# 设置主机名称和本机 IP：
echo "topeet_i.MX6UL/ULL" > /etc/hostname
echo "127.0.0.1 localhost" >> /etc/hosts
echo "127.0.0.1 topeet_i.MX6UL/ULL" >> /etc/hosts

# 配置串口终端
ln -s /lib/systemd/system/getty@.service \
    /etc/systemd/system/getty.target.wants/getty@ttymxc0.service

# 由于我用的是 i.mx6u 的平台，需要添加一个名为 getty@ttymxc0.service 的链接，
# 连接到 getty@.service 服务。i.mx 的平台是 ttymxc0。


# 接下来配置 DHCP，也可以等下载到开发板之后在配置，但启动时会很慢
echo auto eth0 > /etc/network/interfaces.d/eth0 
echo iface eth0 inet dhcp >> /etc/network/interfaces.d/eth0
chmod 777 /etc/network/interfaces.d/eth0 

# 最后我们将退出以后再执行一下 umount.sh 脚本取消挂载

# 打包烧写 Ubuntu
tar -cjf rootfs.tar.bz2 ./*

# 将开发板拨码切换成烧写模式，上电烧写，等待烧写完成。

# Ubuntu 根文件系统测试
# 可以和普通ubuntu一样使用apt来安装gcc等软件，然后编写编译运行
```


## debian文件系统

### 准备工作

```bash
apt-get install -y binfmt-support qemu qemu-user-static debootstrap
```

### 抽取Debain文件系统

```bash
debootstrap --arch=armhf --foreign buster root https://mirrors.tuna.tsinghua.edu.cn/debian/

# 抽取时间比较长，大概 10 分钟左右，大家耐心等待，抽取成功可以看到 linux 的目录树
ls ./root
```

### 完善文件系统

完善文件系统这里我们要使用到 qemu，为什么要用 qemu 呢，因为我们现在是在 X86 虚拟机上操作， 不能完善 arm 的文件系统，所以我们要用 qemu 来模拟 arm 的环境。

```bash
# 复制 qemu-arm-static 到刚构建的基本系统中
cp /usr/bin/qemu-arm-static root/usr/bin

# 初始化文件系统
DEBIAN_FRONTEND=noninteractive DEBCONF_NONINTERACTIVE_SEEN=true \
LC_ALL=C LANGUAGE=C LANG=C chroot root debootstrap/debootstrap --second-stage

# 大概需要 20 分钟左右，大家耐心等待即可

# 使用命令 chroot root，进到我们初始化好的文件系统
chroot root
echo "proc /proc proc defaults 0 0" >> etc/fstab
mkdir -p usr/share/man/man1/
mknod dev/console c 5 1

# 更新下载源，使用命令 vi /etc/apt/sources.list
deb http://mirrors.ustc.edu.cn/debian stable main contrib non-free
deb http://mirrors.ustc.edu.cn/debian stable-updates main contrib non-free

# 然后保存退出，使用命令 apt-get update 更下源
apt-get update

apt-get install vim

adduser user

# 设置 root 密码
passwd root


# 设置以太网
echo "auto eth0" > /etc/network/interfaces.d/eth0
echo "iface eth0 inet dhcp" >> /etc/network/interfaces.d/eth0

# 因为 ull 的性能比较弱，所以我们就不安装桌面了
exit # 退出qemu环境

# 打包文件系统
tar -cjf rootfs.tar.bz2 ./*

# 烧写验证
# 把我们打包好的这个文件系统放到烧写器里面，其他的镜像使用原来的即可，
# 只需要替换文件系统， 然后烧写测试
```

















